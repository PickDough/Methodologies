<Window x:Class="WpfApp1.ApplicationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        xmlns:viewModel="clr-namespace:WpfApp1.ViewModel"
        xmlns:view="clr-namespace:WpfApp1.View"
        mc:Ignorable="d"
        Title="Application" Height="500" Width="725">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:NavigationViewModel}">
            <view:NavigationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:OrderViewModel}">
            <view:OrderView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:MaterialViewModel}">
            <view:MaterialView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:ClientViewModel}">
            <view:ClientView/>
        </DataTemplate>
    </Window.Resources>
    <ContentControl  Content="{Binding   CurrentViewModel}"/>
</Window>
